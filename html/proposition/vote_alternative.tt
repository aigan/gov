[% META title = 'Proposition vote alternative'
%]

[%
   CALL locn(title);
   alt = get(q.param('id'));
   hidden('id', alt.id);
   prop = alt.first_revprop('has_alternative');
%]

<h1>[% prop.wu_jump %]</h1>
<h2>[% alt.name.loc | html %]</h2>

[% IF u.level > 20; alt.wun_jump; END %]

[% IF alt.has_url %]
  <p>
    [% aloc('Discussion url') %]: [% jump(alt.has_url, alt.has_url, href_target='_blank') %]
  </p>
[% END %]

[% alt.has_body.loc %]
